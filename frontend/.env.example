# Copy this to .env.local for local dev overrides (optional - defaults to
# localhost:8000 if not set). For deployment, set VITE_API_URL in your
# hosting platform's environment variables instead (e.g. Vercel project
# settings) - do not commit a real .env file with production URLs.
VITE_API_URL=http://localhost:8000